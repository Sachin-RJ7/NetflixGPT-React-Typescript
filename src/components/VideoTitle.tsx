
type VideoTitleProps = {
    title: string;
    overView: string;
}

const VideoTitle = ({title, overView}: VideoTitleProps) => {
  return (
    <div>VideoTitle</div>
  )
}

export default VideoTitle